plugins {
    id 'com.android.application'
}

android {
    namespace 'com.oid.crash'
    compileSdkVersion 34

    defaultConfig {
        applicationId "com.oid.crash"
        minSdkVersion 26
        targetSdkVersion 34
        versionCode 1
        versionName "1.0"

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
    }

    // DELETED: The entire "signingConfigs" block is removed.
    // Gradle will no longer look for KEYSTORE_FILE or any secrets.

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
            // DELETED: The line "signingConfig signingConfigs.release" is removed.
        }
        debug {
            // Debug builds are signable by default with a temporary key.
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    buildFeatures {
        viewBinding true
    }
}

dependencies {
    implementation 'androidx.appcompat:appcompat:1.6.1'
    implementation 'com.google.android.material:material:1.11.0'
    implementation 'androidx.constraintlayout:constraintlayout:2.1.4'

    // Navigation Components
    implementation 'androidx.navigation:navigation-fragment:2.7.7'
    implementation 'androidx.navigation:navigation-ui:2.7.7'

    // Gson for saving Target App List list to SharedPreferences
    implementation 'com.google.code.gson:gson:2.10.1'

    // Local Storage & File Management helpers
    implementation 'androidx.documentfile:documentfile:1.0.1'

    testImplementation 'junit:junit:4.13.2'
    androidTestImplementation 'androidx.test.ext:junit:1.1.5'
    androidTestImplementation 'androidx.test.espresso:espresso-core:3.5.1'
}